# Internship-docs
# Internship Day 01

## Date
20 January 2026

## Topics Learned
- What is Git
- Git vs GitHub
- Git installation
- git init, status, add, commit

## Commands Practiced
```bash
git init
git status
git add .
git commit -m "Initial commit"
git push

git config --global user.name "name"
git config --global user.email "email@gmail.com"
git config --global core.editor "code --wait"
git config --global core.autocrlf

making a check point or saved point
adding files
staging them
commiting them

satges:
U - untracked 
A - added or staged
C - Commited


command you need to know:
git status -s 
// to know current status of unstaged and staged files
git log --oneline
// to know current status of save points

going back to some previous save point
git reset --hard HEAD~1 (which stage you want to go put number)
git config --list

